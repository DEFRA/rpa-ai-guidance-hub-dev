include:
  - ./service-compose/rpa-ai-guidance-hub-api.yaml
  - ./service-compose/rpa-ai-guidance-hub-ui.yaml
  - ./dependencies/cdp-uploader.yaml
services:
  floci:
    image: floci/floci@sha256:b604da97e8c481c407f8e8d3300a8317d68058ac757dfa2af15fd6c4e9d25c59 # 1.5.19-compat
    ports:
      - '4566:4566'
    env_file:
      - path: .env
        required: false
    environment:
      FLOCI_DEFAULT_REGION: eu-west-2
      FLOCI_STORAGE_MODE: hybrid
      FLOCI_STORAGE_PERSISTENT_PATH: ./data
      # The start.d scripts shell out to `aws`, which needs credentials even
      # against floci. Defaulted here so resource setup works without a .env.
      AWS_REGION: ${AWS_REGION:-eu-west-2}
      AWS_DEFAULT_REGION: ${AWS_DEFAULT_REGION:-eu-west-2}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-test}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-test}
    volumes:
      - ./compose/floci/start.d:/etc/floci/init/start.d:ro
      - floci-data:/app/data
    healthcheck:
      # 99-ready.sh writes /tmp/READY once every start.d script has run, so
      # dependents wait for AWS resource setup, not just for the port to open.
      test:
        - CMD
        - bash
        - -c
        - "curl -s localhost:4566 && cat /tmp/READY"
      interval: 5s
      start_period: 30s
      retries: 15
    networks:
      - rpa-ai-guidance-hub
  mongodb:
    image: mongo@sha256:4510cf3d7050003e958745adb25d2deb3fb907430716162d9cc1a92eda2a6047 # 7.0.28
    ports:
      - '27017:27017'
    volumes:
      - ./compose/mongo:/docker-entrypoint-initdb.d:ro
      # Must match the image's declared VOLUME exactly; a mount on /data is
      # shadowed by an anonymous volume at /data/db and persists nothing.
      - mongodb-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.hello().ok"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - rpa-ai-guidance-hub
  redis:
    image: redis@sha256:3ce533b2b057f74b235d1d8697ae08b1b6ff0a5e16827ea6a377b6365693c7ed # 7.2.3-alpine3.18
    ports:
      - '6379:6379'
    networks:
      - rpa-ai-guidance-hub
volumes:
  floci-data:
  mongodb-data:
networks:
  rpa-ai-guidance-hub:
    name: rpa-ai-guidance-hub
    driver: bridge
